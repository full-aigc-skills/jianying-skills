# 导出门禁参考

## 代理预览

代理预览用于快速审阅主轨、混音和可选烧字幕；它可能不呈现转场、滤镜、蒙版、字体或原生合成效果。报告中写明 `proxy`。

## 原生导出

必须同时满足：受支持的 Runtime Profile、合法安装环境、草稿结构通过、目标级批准、编辑器安全状态、持久任务和输出验证。任何一项缺失都返回 not-ready。

## 结果验证

- 输出路径位于批准目录。
- 文件非空且内容哈希已记录。
- ffprobe 可解析预期视频/音频流和时长。
- 覆盖已有制品时，批准必须明确包含 overwrite。
- 超时后先检查制品和任务审计，禁止盲目重跑。
