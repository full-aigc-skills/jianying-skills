# 失败恢复：字幕导入、样式与校验

场景：执行返回超时或 ambiguous。

1. 不自动重试，也不删除工作副本、快照或外部制品。
2. 使用 job show 与 job audit 查询最后检查点和已发生副作用。
3. 对照输入哈希、输出哈希和 Provider task ID 判断是否已提交。
4. 明确失败时，修复输入并请求绑定新参数的批准；状态未知时保持阻塞。
5. 恢复后重新执行原目标要求的验证门禁，不沿用旧的成功结论。

