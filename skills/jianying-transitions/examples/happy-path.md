# 正常路径：转场目录、边界与播放验收

1. 记录用户目标、输入绝对路径、交付等级和允许的副作用。
2. 运行 jianying --version、doctor --json 与 capabilities --json。
3. 探测相关媒体/草稿事实，生成确定性计划和隔离输出路径。
4. 仅执行 capability 为 supported 且已授权的步骤。
5. 运行结构验证，并按目标补充冷重开、播放或原生导出证据。
6. 返回 task ID、制品路径、证据等级和仍未验证的项目。

