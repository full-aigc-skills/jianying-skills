## 1. 来源门禁

- [x] 1.1 固定研究提交、许可证、允许吸收内容、禁止复制内容和 Rust-only 目标边界，并让包级来源说明可审计。

## 2. 红测

- [x] 2.1 添加缺字段、重复 Recipe、非法步骤、执行字段、按键捕获、无效坐标/时间、路径摘要和未对账派生媒体负例，确认现状失败。

## 3. 辅助制作目录

- [x] 3.1 实现五条版本化 Recipe、责任技能、能力需求、人工门禁、证据目标和 Provider/plan-only 停止路径。

## 4. 领域知识合同

- [x] 4.1 实现 `interaction-events/v1`、`smart-zoom-plan/v1`、`media-preflight/v1` Schema、有效示例和智能缩放/标准化领域参考。

## 5. 技能路由

- [x] 5.1 更新规划、媒体、动效、调节、音频、旁白、字幕、模板和 Harness 的自包含路由，覆盖安全录屏、Web VFX、解说、曝光匹配与 Clone-first。

## 6. 确定性校验

- [x] 6.1 实现辅助制作校验器并通过正例、负例、技能 lint、现有目录和全量回归测试。

## 7. 发布前验证

- [x] 7.1 通过严格 OpenSpec、粒度安装、跨技能链接、禁止运行时扫描和技能内容摘要验证，记录实际证据。

## 8. 不可变发布

- [ ] 8.1 发布不可变 Skills 版本，并由插件 fresh install 锁定摘要后再启用 `add-assisted-media-provider-harness` 消费。
