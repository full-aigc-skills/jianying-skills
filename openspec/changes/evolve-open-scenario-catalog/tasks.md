# 开放目录实施任务

本清单是后续 apply 的实施与验证门禁；规格文件存在不构成任务完成。
依赖配套插件变更 `add-open-workflow-compiler`，不要求先实现插件执行器才能开发目录。
2026-09-21 核对：19/20；逐项证据与未完成边界见 [验证记录](verification.md)。

## 1. 冻结旧合同与扩容失败基线

- [x] 1.1 保存现有 v1 资源、108 个 ID/名称/Recipe/例文摘要和旧消费结果 fixture，验证与当前发布基线一致。（CAT-02）
- [x] 1.2 建立 1/109/1000 项非均分、可变 Recipe/来源数量的 v2 fixture，确认当前实现因缺 v2 支持而失败。（CAT-01）
- [x] 1.3 建立重复身份、未知引用/版本、空目录、越界路径、符号链接、重复 JSON 键和资源超限负例，记录目标失败。（CAT-03/04）

## 2. v2 合同与内容验证

- [x] 2.1 实现版本化目录 JSON Schema，覆盖 SceneCard、Recipe、Source、词表与受限 metadata，并通过正反例。（CAT-01/03、KNOW-01/02）
- [x] 2.2 实现资源大小、普通文件、技能根路径和重复键检查，超限不截断，负例返回结构化错误。（CAT-04）
- [x] 2.3 实现跨 ID/旧别名唯一性、多对多引用及标签/来源/Recipe 完整性校验，通过开放数量用例。（CAT-03）
- [x] 2.4 生成首批 v2 目录与完整旧 ID 映射，复用冻结例文；验证 v1 字节及语义基线未变。（CAT-02、KNOW-01）
- [x] 2.5 实现正式收录与 test_fixture 隔离校验，缺方法来源或热度状态时拒绝正式收录。（KNOW-03）

## 3. 兼容消费与可信分发

- [x] 3.1 输出可供插件测试读取的版本化开发合同向量，明确开发 provenance；不复制另一套可修改规范。（CAT-05）
- [x] 3.2 与插件新版加载器验证兼容矩阵：旧插件/新插件 v1 不回归，v2 完整读取扩容项，未知版本与缺 v2 明确失败。（CAT-01/02）
- [x] 3.3 扩展发布/摘要校验，使 v2、例文与合同资源进入完整性清单，篡改和测试目录不能通过正式门禁。（CAT-04/05）
- [x] 3.4 验证显式开发输入不满足生产 readiness，正式输入失败不回退开发仓或同名网络资源。（CAT-05）

## 4. 三条代表链知识向量

- [x] 4.1 保存 VLOG-01 的 VG01/R01 映射、生活主线、素材缺口和叙事验收，并验证引用完整。（KNOW-04）
- [x] 4.2 保存 COURSE-01 的完整课程/三个预告、双语字幕与语境审校需求，验证 R06/R11 组合没有隐含输出。（KNOW-02/04）
- [x] 4.3 保存 WEDDING-01 的三用途、R03/R02/R04 与独立授权要求，验证多机位缺能力仍为等待条件。（KNOW-02/04）
- [x] 4.4 更新技能路由说明和内容统计，区分候选、正式知识、fixture 与运行证据；检查例文来源和跨技能链接。（KNOW-01/03）

## 5. 发布与回滚证明

- [x] 5.1 执行目录、旧版兼容、技能完整性及文档检查，保存命令、版本、输入摘要和实际结果。（CAT-01 至 CAT-05）
- [x] 5.2 发布兼容技能版本并取得不可变资产、commit 和内容摘要；版本与授权在执行时复核。（CAT-05）
- [x] 5.3 在插件干净安装中完成正式锁定导入和双入口消费验证，不使用开发注入作为正式证据。（CAT-02/05）
- [ ] 5.4 验证恢复旧兼容锁不会改写 Release 或污染 v1；检查本变更与增量规格一致后再同步/归档。（CAT-02/05）
